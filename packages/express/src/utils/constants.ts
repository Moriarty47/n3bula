export const TAG = '@n3bula/express';

export const EMPTY_ARRAY: readonly any[] = Object.freeze([]);

export const EMPTY_OBJECT: Record<any, any> = Object.freeze(Object.create(null));

export const NOOP = () => {};
